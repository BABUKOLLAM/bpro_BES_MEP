<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="cron_generate_invoices" model="ir.cron">
        <field name="name">bpro Billing: Generate Subscription Invoices</field>
        <field name="model_id" ref="model_bpro_billing_subscription"/>
        <field name="state">code</field>
        <field name="code">model._cron_generate_invoices()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="active" eval="True"/>
    </record>
</odoo>
